rootLogger.level = WARN
rootLogger.appenderRef.console.ref = ConsoleAppender

appender.console.type = Console
appender.console.name = ConsoleAppender
appender.console.layout.type = PatternLayout
appender.console.layout.pattern = %d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n

# Flink/Pekko internals are noisy at INFO (MiniCluster startup/shutdown, RPC,
# dispatcher, task executor lifecycle); keep them at WARN so only the app's own
# output (System.out prints) and real problems show up.
logger.flink.name = org.apache.flink
logger.flink.level = WARN

logger.pekko.name = org.apache.pekko
logger.pekko.level = WARN